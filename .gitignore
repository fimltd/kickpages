# Dependencies (the shipped server is bundled into dist/, so deps are not needed at runtime)
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Secrets / local config — never commit
.env
.env.*
kickpages-auth.json
kickpages-creds.json

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Build/temp
*.tgz
tmp/
.cache/

# Internal maintainer docs — not for the public repo
MAINTAINING.md

# NOTE: dist/ is intentionally COMMITTED — it is the shipped, self-contained MCP server.
